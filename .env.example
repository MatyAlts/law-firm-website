# ===================================
# ENVIRONMENT VARIABLES - PRODUCTION
# ===================================

# Database Configuration
DB_USER=lawfirm
DB_PASSWORD=changeme_strong_password_here

# JWT Configuration (min 256 bits)
JWT_SECRET=changeme_super_secret_jwt_key_min_256_bits_please

# Frontend URLs (Update these with your domain)
# For local development:
# NEXT_PUBLIC_API_URL=http://localhost/api
# API_BASE_URL=http://backend:8080
# FRONTEND_URL=http://localhost

# For production (EasyPanel):
NEXT_PUBLIC_API_URL=https://belmontesalafia.com/api
API_BASE_URL=http://backend:8080
FRONTEND_URL=https://belmontesalafia.com

# Server Ports
HTTP_PORT=80
HTTPS_PORT=443

# Environment
NODE_ENV=production
SPRING_PROFILES_ACTIVE=prod

# ===================================
# INITIAL ADMIN CREDENTIALS
# ===================================
# IMPORTANT: These are used ONLY for creating the first admin user
# After deployment, you can:
# 1. Create the first admin through the panel
# 2. Remove these variables from .env for security
# 3. Use the "Change Password" feature in /admin/users
INITIAL_ADMIN_EMAIL=admin@lawfirm.com
INITIAL_ADMIN_PASSWORD=changeme123
INITIAL_ADMIN_ROLE=superadmin
